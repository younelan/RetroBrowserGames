# Mastermind

Vanilla JS Mastermind (mobile-first).

How to run:

Open `Mastermind/index.html` in a browser. Use the palette or tap a peg to open the inline picker. Fill the 4 pegs then press `Validate` on the active row or the global `Submit` button. `Clear` resets the current row. `Restart` starts a new game.
